import styled from "styled-components";

export const BackIconContainer = styled.div`
  position: absolute;
  left: 1.2rem;
  top: 0.3rem;
  font-size: 2.5rem;
  cursor: pointer;
  transition: all 0.2s;
  &:hover {
    transform: scale(1.05);
  }
  color: #282f3f;
`;
